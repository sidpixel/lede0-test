name: test

on:
  push:
    branches: 
      - main
    paths:
      - '.github/workflows/test.yml'

jobs:
  check_macos:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: sys info
        run: |
          brew install  coreutils findutils gnu-sed gawk gnu-tar grep gpatch  git make gcc  xargs fileutils awk getopt  realpath install
          df -h