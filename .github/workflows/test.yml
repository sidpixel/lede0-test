name: test

on:
  push:
    branches: 
      - main
    paths:
      - '.github/workflows/test.yml'

jobs:
  cleanup:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: sys info
        run: |
          nproc
          lscpu
